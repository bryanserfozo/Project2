const search_btn = document.getElementById('search-btn');
const search_results_wrapper = document.getElementById(
	'search-results-wrapper'
);
const bottom = document.getElementById('bottom');
const toggle_button = document.getElementById('toggle');

toggle_button.addEventListener('click', () => {
	bottom.classList.toggle('hide');
});

const API_URL = search_btn.addEventListener('click', () => {
	let search_results_wrapper = document.createElement('div');
});
